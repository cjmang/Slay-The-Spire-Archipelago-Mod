pluginManagement {
    plugins {
        id 'com.github.johnrengelman.shadow' version '6.0.0'
    }
}

rootProject.name = 'StSArchipelago'

//include ':APClient'
//project(":APClient").projectDir = file("../APClient")